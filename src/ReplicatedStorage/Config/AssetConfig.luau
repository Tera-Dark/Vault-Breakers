--!strict
-- Leave these blank to use the built-in UI click fallback. Nothing requires an external asset.
-- Upload the original WAVs in art/audio under the experience owner, then paste their IDs here.
return table.freeze({
	Sounds = {
		Click = "rbxasset://sounds/switch.wav",
		Offer = "", Breach = "", Heartbeat = "", Tick = "", Reveal = "", Claim = "",
	},
	Volume = 0.45,
})
